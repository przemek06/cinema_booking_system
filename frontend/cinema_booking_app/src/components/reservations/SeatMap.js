
// reservations - already taken seats
// chosenSeats - seats chosen by the user
// setChosenSeats - update seats chosen by the user
const SeatMap = (reservations, chosenSeats, setChosenSeats) => {

    return (
        <>
        
        </>
    )
}

export default SeatMap